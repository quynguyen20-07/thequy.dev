{
  "name": "quynguyen-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend-express"
  ],
  "scripts": {
    "dev:ui": "yarn workspace frontend dev",
    "dev:server": "yarn workspace backend-express dev",
    "build": "yarn workspace frontend build && yarn workspace backend-express build",
    "start": "yarn workspace backend-express start",
    "migrate": "yarn workspace backend-express migrate",
    "dev:all": "concurrently \"yarn dev:ui\" \"yarn dev:server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}